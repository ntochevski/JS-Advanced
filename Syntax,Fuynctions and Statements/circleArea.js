function solve(input) {
    // let result = '';
    // typeof (input) === 'number' ? result = (Math.pow(input, 2) * Math.PI).toFixed(2) :
    //     result = `We can not calculate the circle area, because we receive a ${typeof input}.`;
    // console.log(result);
    let arr = [1, 2, 3]
    let newww = arr.reduce((acc, num) => acc.push(num), []);
    console.log(newww);
}

solve('name')